//history allows us to manipulate the DOM
//we use it with Router instead of BrowserRouter
//it allows us to dictate what page to go after an action
import { createBrowserHistory } from 'history';


export default createBrowserHistory();